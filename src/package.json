{
  "name": "node-assignment-template",
  "version": "1.0.0",
  "description": "Minimal take-home assignment starter with Express and ORM setup",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "sequelize db:migrate --url \"$DATABASE_URL\"",
    "db:seed": "sequelize db:seed:all --url \"$DATABASE_URL\"",
    "test": "npm run db:migrate && npm run db:seed && node --test --test-concurrency=1 test/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.23.0",
    "sequelize": "^6.37.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "sequelize-cli": "^6.6.3"
  }
}
